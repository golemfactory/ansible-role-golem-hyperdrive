---
# defaults file for golem-hyperdrive

hyperdrive_extra_args: ""
hyperdrive_path: "{{ hyperdrive_user_home }}"
hyperdrive_release_url: "https://github.com/golemfactory/simple-transfer/releases/download/{{ hyperdrive_version }}/hyperg_{{ hyperdrive_version }}_linux-x64.tar.gz"
hyperdrive_user: golem
hyperdrive_user_group: "{{ hyperdrive_user }}"
hyperdrive_user_home: "/home/{{ hyperdrive_user }}"
hyperdrive_version: 0.3.6
